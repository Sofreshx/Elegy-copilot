# Aspire Context
Capture how Aspire is used for testing and orchestration in the host repo.

Fill this during onboarding:
- Services and profiles:
- Test harness patterns (fixtures, seeding, isolation):
- Compose/env integration:
- Observability/logging conventions:
- CI considerations:
- Known pitfalls (also log in `warnings.md` if critical):
