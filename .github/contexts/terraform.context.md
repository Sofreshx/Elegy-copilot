# Terraform Context
Use to capture provider conventions, module structure, backends, tagging, environments, and CI/CD practices for Terraform in the host repo.

Fill this during onboarding:
- Providers/backends:
- Environments and workspaces:
- Module layout and naming:
- Tagging/labels policy:
- State management rules:
- Validation/formatting commands:
- Known pitfalls (also add to `warnings.md` if critical):
