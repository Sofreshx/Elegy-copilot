appId: dev.instructionengine.desktop
productName: Instruction Engine
directories:
  output: release
  buildResources: resources
files:
  - dist-electron/**
  - server.js
  - lib/**
  - routes/**
  - ui-dist/**
  - public/**
  - package.json
extraResources:
  - from: ../engine-assets
    to: engine-assets
    filter:
      - '**/*'
  - from: ../local-tracker/src
    to: local-tracker/src
    filter:
      - '**/*'
  - from: ../local-tracker/node_modules
    to: local-tracker/node_modules
    filter:
      - '**/*'
  - from: ../local-tracker/package.json
    to: local-tracker/package.json
  - from: ../local-tracker/tsconfig.json
    to: local-tracker/tsconfig.json
  - from: ../scripts
    to: scripts
    filter:
      - cli-*.ps1
      - cli-*.sh
      - mcp-env.ps1
      - mcp-env.sh
      - validate-policy-lockfiles.js
      - vscode-settings-patch.mjs
extraMetadata:
  main: dist-electron/main.js
win:
  target:
    - nsis
  artifactName: instruction-engine-${version}-${os}-${arch}.${ext}
publish:
  provider: github
  owner: ${env.GITHUB_REPOSITORY_OWNER}
  repo: ${env.GITHUB_REPOSITORY_NAME}
  releaseType: draft
