# .cli/

Contains the install contract for the engine.

- `manifest.allowlist.json` — security allowlist for install validation

All canonical assets (agents, skills, prompts, instructions) live in `engine-assets/`, including `manifest.json`.

The dashboard UI previously at `.cli/ui/` has been moved to `copilot-ui/` at the repo root.
