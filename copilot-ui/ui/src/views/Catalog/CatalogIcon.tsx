// Re-export from the shared AppIcon component for backward compatibility.
// Prefer importing AppIcon from '../../components/AppIcon' directly.
export { default, type AppIconName as IconName } from '../../components/AppIcon';
