---
name: marten-linq-querying
description: "Marten LINQ querying (documents). Prevent queries that compile but fail at runtime (NotSupportedException). Use this when writing/debugging Marten LINQ filters, operators, Include(), child collections (Any/Contains), pagination (Skip/Take, OrderBy, ToPagedListAsync, Stats), or ToAsyncEnumerable()."
---

# Skill: marten-linq-querying

This is the discovery entrypoint for this skill.

Full instructions live in: .github/skills/marten-linq-querying/SKILL.md

When applying this skill, open the full file above and follow its workflow.
